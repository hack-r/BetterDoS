version: "3.9"

services:
  betterdos:
    # image: ghcr.io/mhprodev/mhddos:latest
    build: .
    container_name: betterdos
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json
      - ./files:/app/files
     
    
    
